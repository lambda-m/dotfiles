# Brewfile.macmini - common plus whatever this machine actually needs.
#
#   brew bundle install --file=brew/Brewfile.macmini
#
# Deliberately thin. Fill it during the survey on that machine:
#   brew bundle cleanup --file=brew/Brewfile.macmini      # dry run: what is here but unlisted
#   brew bundle check   --file=brew/Brewfile.macmini -v   # what is listed but missing
# then promote the keepers with
#   brew bundle add --cask X --file=brew/Brewfile.macmini       (this machine only)
#   brew bundle add --cask X --file=brew/Brewfile.common   (all machines)

dir  = File.expand_path(File.dirname(__FILE__))
path = "#{dir}/Brewfile.common"
instance_eval(File.read(path), path)

# ---------- casks: this machine only ----------
